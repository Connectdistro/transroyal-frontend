/**
 * The persistent world scene graph (Production Handbook Sections 8, 14).
 * Placeholder only — environment geometry is a later production task.
 */
export class World {
  constructor(experience) {
    this.experience = experience;
  }

  update() {
    // Reserved for scene content driven by the scroll engine — none yet.
  }
}
